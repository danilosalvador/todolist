import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
        justifyContent: 'center',
    },
    icon: {
        margin: 30,
    },
    title: {
        color: '#808080',
        fontSize: 14,
        fontWeight: 'bold',
    },
    helper: {
        color: '#808080',
        fontSize: 14,
    }
});
